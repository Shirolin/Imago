# 管线架构可行性与体验深度评估报告

除了代码实现层面的缺漏，从**产品交互体验**和**底层技术可行性**来看，这份《混合管线架构》存在几个致命的硬伤。

**结论先行：强烈建议放弃该管线计划，改走「独立功能、即开即用」模式。**

---

## ⛔ 核心论证：为什么这条路走不通/体验不好？

### 致命伤 1：画质级联损耗 (Generation Loss)

- **原理缺陷**：现有的管线在每一步（如：裁剪 -> 滤镜 -> 尺寸调整）都在生成新的 `Blob` 作为下一阶段的基底。
- **体验灾难**：基于 Canvas API 的导出，如果是 JPEG/WebP，**每经过一个页面，图片都会被重新解码并有损编码一次**。用户经历 3 道工序后，原本清晰的图片会出现不可逆的噪点、色彩断层和模糊。真正的“非破坏性管线”（如 Lightroom或Figma）是积攒操作参数，在最后一步一次性对原图进行渲染。

### 致命伤 2：反人类的“隐式保存”与撤销机制

- **设计谬误**：“离开即定稿”听起来很优雅，但完全违背用户的试错直觉。用户经常只是点去另一个页面看看，结果刚才随手的调整就被**强制归档（覆盖）**了。
- **缺乏步进撤销**：架构只设计了 [revertToOriginal](file:///d:/Project/Imago/src/stores/imageStore.ts#138-150)（全量回退）。这意味着用户做了 A -> B -> C 三步，如果在 C 步跳转错了，他**无法撤销 C**，只能全部清空，从原图重做。用户会极其崩溃。

### 致命伤 3：1v1 模型的破缺 (分割 & 拼接)

- **逻辑断裂**：该管线架构默认是 `1 Input -> 1 Output`。但 Imago 包含特殊工具：
  - **图片分割 (Split)**：1 张图进去，9 张图出来。离开页面时，管线如何把 9 张图塞进下一道工序的 `currentWorkingBlob`？完全无解。
  - **长图拼接 (Combine)**：多张图进去，1 张图出来。这更是彻底打破了基于单张 [ImageItem](file:///d:/Project/Imago/src/stores/imageStore.ts#4-40) 流转的管线设计。

### 致命伤 4：极高的内存踩踏风险 (OOM)

- **性能危机**：以 Web 端的限制，如果用户一次性上传 30 张高分辨率大图（如单张 10MB）。在各个页面跳转时，[ImageItem](file:///d:/Project/Imago/src/stores/imageStore.ts#4-40) 上不断挂载巨大的 `currentWorkingBlob` 和生成的长效 `ObjectURL`。浏览器的 JS 内存极易突破上限导致页面崩溃白屏。前端的 Store 不适合作为重型物理文件（Blob）的接力中枢。

---

## 🎯 破局方案：退一步海阔天空

放弃强耦合的黑盒流转，将 Imago 降维成类似 ILoveIMG / TinyPNG 的**独立聚合工具箱**。

### 方案 A：纯粹独立模式（最推荐）

- **心智**：每个页面是一个独立车间。用户进来就是为了单一目的，做完、下载、结束。
- **架构**：彻底砍掉 `currentWorkingBlob`、`pipelineBasePreview` 等复杂概念。Store 里的 [ImageItem](file:///d:/Project/Imago/src/stores/imageStore.ts#4-40) 只存储 `{ file, preview, status }`。各个页面独立处理 `file`，互不干扰。开发成本极低，绝对没有任何 bug 和内存泄露。

### 方案 B：显式转移模式（妥协方案）

- 如果真的很想把裁剪结果拿去压缩怎么办？
- **改造**：在完成裁剪的“下载”按钮旁边，加一个 **“添加至...” (Add to other tool)** 按钮。点击后，将当前内存中的 `processedBlob` 打包成一个新的 `File`，自动跳转并将其作为独立的新任务插入目标的列表中。这既保留了灵活性，又彻底切断了状态树上的耦合噩梦。
