# Imago · 暖纸

浏览器里的本地图片工作台。奶油画布，陶土一点，衬线字标。检查器是工具，不是营销页。

味道对齐 Claude / Claude Code 产品语言：奶油画布、珊瑚陶土、衬线字标、细线、密控件。不搬 Tweaks 栏、Stone/Moss 调味盘、Mobile/Web 分段。

暖纸是内部味道卡名。产品 chrome 只写 Imago，不要把暖纸印在侧栏或封面。

## Tokens（代码真源，禁止另发明 hex）

- `--paper: #FAF9F5` 画布/房间
- `--board: #F5F0E8` 侧栏、检查器（比画布深一档，不是浮白）
- `--well: #EFE9DE` 纸井（空态和画布落点）
- `--ink: #141413` 近黑暖墨
- `--muted: #6C6A64`
- `--accent: #CC785C` 唯一强调，珊瑚。没有第二品牌色（不要薄荷、不要徽章青绿、不要黄铜 `#c4a574`）
- `--accent-press: #A9583E` 按下
- `--danger: #C64545` 只给删除
- `--hairline: #E6DFD8` 分割线。禁止粗描边、禁止彩色描边
- `--product: #181715` 图卡片等产品面，奶油上的深海军。不跟主题反色
- `--on-product: #FAF9F5`
- `--radius-well: 20px` 纸井。工作台，不是杂志 32px
- `--radius-ctrl: 8px` 控件。不要药丸
- 字体：字标、封面工单、空态那一行用 Noto Serif SC，字重 500，字距约 `-0.02em`。控件和规格用 Noto Sans SC，字重 400–500。禁止 Inter / Geist / Roboto / Arial / IBM Plex
- 栏不透明。禁止玻璃、blur、glow orb、彩色大投影。纸板无投影，或最多 `0 1px 2px rgba(20,20,19,0.06)`

## 深色

`data-theme=dark` 用海军产品面，不是旧灯箱。封面和工作台房间锁死浅色纸，产品 chrome 没有主题开关。

- `--paper: #181715`
- `--board: #252320`
- `--well: #1F1E1B`
- `--ink: #FAF9F5`
- `--muted: #A09D96`
- `--hairline: #3A3834`
- `--product: #181715` 图卡片面，比 board 深一档
- `--accent` 不变

## 封面（第一拍）

打开应用是封面，不是压缩空井，没有侧栏，没有九宫格工具卡。

- 顶栏：衬线 Imago 左，「本地 · 不上传」右。没有太阳/月亮主题开关。房间锁死浅色纸。
- 中央：一张竖版快照裱在 `--board` 上，宽卡纸边约 48–64px，圆角用 `--radius-well`。照片必须像能拿去压缩/裁切的手持快照。禁止陶瓷静物、干枝、生活方式产品卡、空井文案。封面已有印样，「把图放到这张纸上」不出现在这一页。
- 右侧：排印工单，不是卡片。衬线「工单」，然后 № 1042。竖列，陶土 1px 装订线在左。没有图标、没有 bento、没有功能简介。
- 视口正好露出 4 步；列表可滚；进出条目用 `mask-image` 渐隐。8 个工具都在这张单上。
- 文案锁定，不要暗房黑话（原片/压印/洗信息）：01 压缩 / 02 裁切 / 03 去拍摄信息 / 04 拆分 / 05 拼接 / 06 去背景 / 07 滤镜 / 08 做图标
- 右下：陶土「开始处理」，圆角 `--radius-ctrl`。不要粘在印样上。

点「开始处理」进入工作台（侧栏 + 纸井）。空井「把图放到这张纸上」是第二拍。工作台侧栏字标只写 Imago。

## 列表卡（工作台）

井上的是裱好的印样：宽奶油边（约 24–32px）、浅接触影。文件名写在照片下面。规格（源体积、默认输出、质量）只在右侧检查器，不印在照片上。不要深炭灯箱片，不要陶土裁切框围照片。

## 签名动效

图落到纸井上，纸铺平（translateY 8px → 0 + 透明度，280ms ease-out）。封面工单滚动安静，渐隐是唯一额外。不要揭起、不要灯箱闪光、不要 bounce、不要 hover 上浮。

## Keep

- 本地处理、Web Worker、队列、Abort
- 压缩 / 裁切 / 去拍摄信息 / 拆分 / 拼接 / 去背景 / 滤镜 / 做图标（工作台另有尺寸）
- 列表台 vs 画布台
- 棋盘格
- 规格数字 tabular
- 中文操作文案，i18n 结构
- 密度：检查器是工具台。大圆角和大留白只给纸井和封面装裱，不把工作台做成营销画布

## Forbid

- 深色观察室、灯箱那套 `--room #12151a` / `--accent #1e7a8a`
- Inter、薄荷+琥珀、彩虹工具色、Sparkles、英雄区、九宫格功能卡
- 把 Claude Design 的布局当稿来描
- 暖铜金属、工艺台揭起
- 加倍整页留白、Geist、玻璃
- `font-black`、控件 `uppercase tracking-widest`、主按钮投影、`hover:-translate-y`
- 产品 chrome 写暖纸、主题开关当封面戏服

## 文案

操作语言。封面：「开始处理」。空井：「把图放到这张纸上」。字标是衬线的 Imago，不要「隐私至上」，不要暖纸副标。
